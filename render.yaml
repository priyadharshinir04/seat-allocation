services:
  - type: web
    name: seat-allocation
    env: python
    envVars:
      - key: PYTHON_VERSION
        value: "3.12"
    buildCommand: ./build.sh
    startCommand: gunicorn app:app
    plan: free
